import React, { useEffect } from "react";
import { Container } from "react-bootstrap";

const HomePage: React.FC = () => {
  useEffect(() => {}, []);

  return (
    <Container fluid>
      <h1 className="text-center">LOTUS RP</h1>
    </Container>
  );
};

export default HomePage;
